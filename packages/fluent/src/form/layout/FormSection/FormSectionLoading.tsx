import { FC } from 'react';

// import FormControl from '../../FormControl';

interface FormSectionLoadingProps {
  controlCount: number;
}

export const FormSectionLoading: FC<FormSectionLoadingProps> = ({
  controlCount,
}) => {
  return null;
};
